{
  "name": "@agenti-fy/setup",
  "version": "0.3.5",
  "description": "Interactive setup wizard for agenti-fy. Registers nine GitHub Apps (one per built-in persona) via the App Manifest flow, installs them on the target repo, collects Anthropic credentials, and writes a ready-to-use .env. Generates a standalone docker-compose.yml + souls/ alongside the .env so operators can run `docker compose up` without cloning the source repo. Run with `npx @agenti-fy/setup init`.",
  "license": "MIT",
  "author": "agenti-fy contributors",
  "homepage": "https://github.com/agenti-fy/core#readme",
  "bugs": {
    "url": "https://github.com/agenti-fy/core/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agenti-fy/core.git",
    "directory": "packages/setup"
  },
  "keywords": [
    "agenti-fy",
    "setup",
    "wizard",
    "github-app",
    "cli"
  ],
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "type": "module",
  "bin": {
    "agentify-setup": "./dist/bin.js"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && pnpm copy-assets",
    "copy-assets": "mkdir -p dist/souls && cp -f ../../souls/*.md dist/souls/",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/bin.js",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@agenti-fy/shared": "workspace:*",
    "@octokit/auth-app": "^7.1.1",
    "@octokit/rest": "^21.0.2",
    "undici": "^7.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.7.2"
  }
}
